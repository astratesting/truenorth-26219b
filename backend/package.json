{
  "name": "truenorth-backend",
  "version": "1.0.0",
  "description": "TrueNorth backend API - Express.js server for waitlist and health check endpoints",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "dev": "node --watch main.js",
    "test": "jest --testEnvironment=node --detectOpenHandles --forceExit",
    "lint": "eslint . --ext .js --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
